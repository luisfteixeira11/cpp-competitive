#include <iostream>
using namespace std;

int main(){
    int condição, n;


    while(condição){
        // Bloco de código que será executado enquanto a condição for verdadeira.
    }

    do{
        // Bloco de código que será executado, se repetindo enquanto a condição do while for verdadeira.
    } while(condição);

    for (int i=0; i<n; i++){  
        // Bloco de código que será executado "n" vezes;
    } // ou seja: for (inicializador; condição para fim da repetição; manipulação do inicializador)


}